import 'package:flutter/material.dart';

class ProfilBlock extends StatelessWidget {
  const ProfilBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        CircleAvatar(),
      ],
    );
  }
}
